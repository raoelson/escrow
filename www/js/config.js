app.config(['$mdThemingProvider','$mdDateLocaleProvider','uiGmapGoogleMapApiProvider',function($mdThemingProvider,$mdDateLocaleProvider,GoogleMapApi) {      var orangeTheme = $mdThemingProvider.extendPalette('orange', {    'contrastDefaultColor': 'light'  });   var blueTheme = $mdThemingProvider.extendPalette('blue', {    '50' : '51aec2' ,// cyan classe    'contrastDefaultColor': 'light',    'contrastStrongLightColors': ['50']  });    $mdThemingProvider.definePalette('orangeLight', orangeTheme);    $mdThemingProvider.definePalette('blueLight', blueTheme);    $mdThemingProvider.theme('default')             .primaryPalette('blueLight',                 { 'default' : '900',                  'hue-2': '50'})             .accentPalette('orangeLight'                );    $mdDateLocaleProvider.formatDate = function(date) {        if(date) {            var day = date.getDate();            var monthIndex = date.getMonth();            var year = date.getFullYear();            return day + '/' + (monthIndex + 1) + '/' + year;        }        else {            return '';        }    };    GoogleMapApi.configure({     key: 'AIzaSyCbRPhVlxgVwBC0bBOgyB-Dn_K8ONrxb_g',    // v: '3.20',    libraries: 'weather,geometry,visualization'  });}]);